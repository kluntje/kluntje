{
  "name": "root",
  "private": true,
  "scripts": {
    "create-package": "lerna create",
    "publish": "lerna run build && lerna publish",
    "build": "lerna run build",
    "test": "lerna run test",
    "bootstrap": "lerna bootstrap",
    "add": "lerna add @pv-fe/js-utils --scope=@pv-fe/wc-core"
  },
  "devDependencies": {
    "@types/jest": "24.0.23",
    "@types/node": "12.12.14",
    "jest": "24.9.0",
    "lerna": "3.19.0",
    "rimraf": "3.0.0",
    "ts-jest": "24.2.0",
    "typescript": "3.7.2"
  }
}
